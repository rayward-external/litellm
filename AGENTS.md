Read @CLAUDE.md for coding guidelines

Rayward fork rule: open PRs against `rayward-external/litellm` only, never
`BerriAI/litellm`. See the 'Rayward fork' section at the top of @CLAUDE.md.
`gh` defaults to upstream here — always pass `--repo rayward-external/litellm`.
